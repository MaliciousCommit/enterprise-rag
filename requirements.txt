# requirements.txt
#
# Module 1 — Naive RAG foundations
# Pin exact versions in production. Use >= ranges for development.
#
# Install: pip install -r requirements.txt

# ── LLM & Embeddings ────────────────────────────────────────────────────────
openai>=1.30.0              # OpenAI API client (gpt-4o + text-embedding-3-small)
tiktoken>=0.7.0             # Token counting (enforces context window budgets)

# ── Vector Database ──────────────────────────────────────────────────────────
qdrant-client>=1.9.0        # Qdrant Python client (REST + gRPC)

# ── Reliability ─────────────────────────────────────────────────────────────
tenacity>=8.3.0             # Retry logic with exponential backoff
                            # (OpenAI 429/500 errors happen in prod)

# ── Configuration ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # Load .env file into environment variables
pydantic>=2.7.0             # Data validation and settings management
                            # (We use Pydantic v2 throughout — different API from v1)

# ── CLI & Output ─────────────────────────────────────────────────────────────
rich>=13.7.0                # Beautiful terminal output for scripts
                            # (colored tables, progress bars, panels)

# ── Future phases (commented out — uncomment as we reach each phase) ─────────

# Phase 4 (LangGraph):
# langgraph>=0.1.0
# langchain-core>=0.2.0

# Phase 4 (PostgreSQL checkpointing):
# langgraph-checkpoint-postgres>=1.0.0
# psycopg[binary]>=3.1.0

# Phase 6 (Redis caching):
# redis>=5.0.0
# upstash-redis>=1.0.0

# Phase 8 (Guardrails):
# llm-guard>=0.3.0
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0

# Phase 9 (Observability):
# langsmith>=0.1.0
# opentelemetry-sdk>=1.24.0
# prometheus-client>=0.20.0

# Phase 10 (API server):
# fastapi>=0.111.0
# uvicorn[standard]>=0.29.0
